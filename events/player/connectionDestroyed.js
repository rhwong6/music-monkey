const { updateGui } = require('../discord/interactionCreate');

module.exports = {
    name: "connectionDestroyed",
    execute() {
        /*
        console.log('THIS RUNS RIGHT?');
        updateGui('something random', 'play', 'player connection end', true);
        console.log('???????');
        */
    }
}